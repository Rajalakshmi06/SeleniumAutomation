package week3.assignments;

public interface Hardware {
	
	//achieve 100% abstraction.
	//always define only abstacrt classes.
	//so we can declare the methods only without body
	//declare only final an static keywords.default it take final if not declared
	
	int a=10;
	 public void hardwareResources();

	int softwareResources();

	}
